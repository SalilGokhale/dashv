export class Technology {
  name;
  versionNumber;
  versionLastDate;

  constructor(name, versionNumber, versionLastDate) {
    this.name = name;
    this.versionNumber = versionNumber;
    this.versionLastDate = versionLastDate;
  }
}